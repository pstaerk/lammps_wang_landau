# Analysis

Post-processing helpers are in `analysis/scripts/`.

Recommended outputs to analyze:

- log density of states
- histogram flatness history
- convergence of modification factor
- derived thermodynamics (if produced)

Store sample small output in `analysis/test_data/` for reproducible checks.
